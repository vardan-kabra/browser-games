# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp

# Dependencies (none today, but just in case)
node_modules/

# Local scratch / test files
_test.js

# Atomic-write / editor temp files (e.g. CLAUDE.md.tmp.1234.abcd)
*.tmp.*

# Machine-specific Claude config (preview launch config has local paths/ports)
.claude/launch.json

# Working screenshots — kept on disk, not in the repo
UI Screenshots/

# Screenshot workflow (Puppeteer captures)
temporary screenshots/

# Skill-creator eval scratch (test runs for the tune-29-ai skill — not part of the skill itself)
.claude/skills/*-workspace/
