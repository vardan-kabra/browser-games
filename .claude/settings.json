{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "git pull --ff-only origin main 2>&1 || echo \"[SessionStart] git pull FAILED — session continued anyway\"",
            "statusMessage": "Pulling latest from GitHub...",
            "timeout": 60
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "git add -A && (git diff --cached --quiet || git commit -q -m \"Auto-commit on session end: $(date '+%Y-%m-%d %H:%M:%S')\") && git push -q origin main || true",
            "statusMessage": "Auto-committing and pushing to GitHub...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
